'use client'; import { useState } from 'react'; import { useRouter } from 'next/navigation'; import { loginRequest } from '@/lib/auth/api'; import { setSession } from '@/lib/auth/storage'; export default function LoginForm(){const router=useRouter();const [form,setForm]=useState({email:'',password:''});const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function handleSubmit(e){e.preventDefault();setError('');setLoading(true);try{const data=await loginRequest(form);setSession({token:data.accessToken,user:data.user});router.replace('/dashboard/portfolio');}catch(err){setError(err.message||'Login failed');}finally{setLoading(false);}} return <form className='authForm' onSubmit={handleSubmit}><label className='authLabel'>Email<input className='authInput' type='email' value={form.email} onChange={e=>setForm({...form,email:e.target.value})} required /></label><label className='authLabel'>Password<input className='authInput' type='password' value={form.password} onChange={e=>setForm({...form,password:e.target.value})} required /></label>{error?<div className='authError'>{error}</div>:null}<button className='authPrimary' type='submit' disabled={loading}>{loading?'Signing in...':'Login'}</button></form>;}
