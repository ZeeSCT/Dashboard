import StatusBadge from './StatusBadge'; import ProgressCell from './ProgressCell'; export default function DataTable({columns,rows}){return <div className='tableWrap'><table><thead><tr>{columns.map(c=><th key={c.key}>{c.label}</th>)}</tr></thead><tbody>{rows.map((row,index)=><tr key={row.id||index}>{columns.map(c=>{const value=row[c.key]; if(c.type==='status') return <td key={c.key}><StatusBadge tone={value?.tone||'gray'}>{value?.label||value}</StatusBadge></td>; if(c.type==='progress') return <td key={c.key}><ProgressCell value={value?.value||0} color={value?.color} /></td>; return <td key={c.key}>{value}</td>;})}</tr>)}</tbody></table></div>;}
