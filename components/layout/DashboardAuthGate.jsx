'use client'; import { useEffect, useState } from 'react'; import { usePathname, useRouter } from 'next/navigation'; import { clearSession, getSession } from '@/lib/auth/storage'; export default function DashboardAuthGate({children}){const router=useRouter();const pathname=usePathname();const [ready,setReady]=useState(false);useEffect(()=>{const session=getSession();if(!session?.token){clearSession();router.replace(`/login?next=${encodeURIComponent(pathname||'/dashboard/portfolio')}`);return;} setReady(true);},[router,pathname]); if(!ready)return <div style={{padding:24}}>Checking session...</div>; return children;}
