'use client'; import Link from 'next/link'; export default function SidebarSection({title,items,isOpen,onToggle,pathname}){return <div className='sidebarSection'><button className='sectionToggle' onClick={onToggle} type='button' aria-expanded={isOpen}><span>{isOpen?'▾':'▸'}</span><span>{title}</span></button>{isOpen?<div className='sectionItems'>{items.map(item=>{const href=`/dashboard/${item.slug}`;const active=pathname===href;return <Link key={item.slug} href={href} className={`menuLink ${active?'active':''}`}><span className='dot' style={{background:item.color}} /><span>{item.label}</span></Link>;})}</div>:null}</div>;}
