import { notFound } from 'next/navigation'; import ScreenRenderer from '@/modules/dashboard/components/ScreenRenderer'; import { screensBySlug } from '@/modules/dashboard/data/screens'; export default function DashboardScreenPage({params}){const screen=screensBySlug[params.slug]; if(!screen) notFound(); return <ScreenRenderer screen={screen} />;} export async function generateStaticParams(){return Object.keys(screensBySlug).map(slug=>({slug}));}
