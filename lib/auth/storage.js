const SESSION_KEY='scientechnic_session';const COOKIE_KEY='scientechnic_token';export function getSession(){if(typeof window==='undefined')return null;const raw=window.localStorage.getItem(SESSION_KEY);if(!raw)return null;try{return JSON.parse(raw)}catch{return null}} export function setSession(session){if(typeof window==='undefined')return;window.localStorage.setItem(SESSION_KEY,JSON.stringify(session));if(session?.token){document.cookie=`${COOKIE_KEY}=${encodeURIComponent(session.token)}; path=/; max-age=${60*60*24*7}; samesite=lax`;}} export function clearSession(){if(typeof window==='undefined')return;window.localStorage.removeItem(SESSION_KEY);document.cookie=`${COOKIE_KEY}=; path=/; max-age=0; samesite=lax`;} export function isAuthenticated(){const s=getSession();return Boolean(s?.token);}
